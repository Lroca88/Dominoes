@model IEnumerable<Dominoes.Models.UserProfileInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GroupAdministered)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GroupAdministered)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserProfileInfoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserProfileInfoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserProfileInfoID })
        </td>
    </tr>
}

</table>
